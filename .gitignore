# 二进制是**要提交**的（dist/ 不在忽略之列）：
# 服务器上只需要 curl 一个 deploy.sh 就能装，不用装 Go、不用配 Token。
#
# 不用担心仓库膨胀。git 对同一路径的历次版本做 delta 压缩，而 Go 静态
# 二进制版本之间绝大部分（runtime、标准库）是相同的。实测连续 8 次发布：
# 每版只改版本号 -> 仓库零增长；每版新增一批功能代码 -> 总共只涨 0.2MB。
# 8 版二进制原始总量 88MB，仓库实际 5.1MB。
#
# 真正会让它变大的是换 Go 版本（runtime 整体变了，delta 失效，那一版
# 实打实多约 5MB）。一年换两次也就多 10MB。

# 直接 go build 在项目根目录留下的产物，不要提交（dist/ 里那份才是）。
/ipv6-proxy
*.exe

# 本地口令，绝不入库
.env

# release.sh 时代的残留：dist/ 下曾经放过 deploy.sh 和 .env.example 的副本。
# 副本会和根目录的正本悄悄不同步——只留正本。
/dist/deploy.sh
/dist/.env.example
