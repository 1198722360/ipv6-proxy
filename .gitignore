# 二进制是**要提交**的（dist/ 不在忽略之列）：
# 服务器上只需要 git clone 就能拿到 deploy.sh 和对应架构的二进制，
# 不用装 Go、不用配 Token。代价是每次重新提交二进制会给仓库永久增加
# 约 11MB（两个架构），git 历史删不掉——提交频率高的话要留意。

# 直接 go build 在项目根目录留下的产物，不要提交（dist/ 里那份才是）。
/ipv6-proxy
*.exe

# 本地口令，绝不入库
.env

# release.sh 时代的残留：dist/ 下曾经放过 deploy.sh 和 .env.example 的副本。
# 副本会和根目录的正本悄悄不同步——只留正本。
/dist/deploy.sh
/dist/.env.example
